# Eagle Wallet - Claude Code Instructions

## 工作风格
- 用中文沟通
- 直接执行任务，不需要过多确认
- 文件修改、代码编辑等操作直接执行，不用询问

## Git 推送规则
- **绝对不要自动推送代码**，必须等用户明确下达推送指令才执行 git push
- 提交 commit 可以自主进行，但 push 必须等指令

## 代码修改规则
- 修复或添加功能时，必须完整查看所有相关联的代码和页面
- 不准只改一部分就跳过，要彻底检查上下游关联的文件
- 针对上下文内容做精准修复，不做无关改动
- 修改代码后如果出现错误，必须立即修复，不能留下问题
- 不偷懒，不省略，确保每次改动都完整且正确
