import { DeviceEventEmitter } from 'react-native';

export const EVENT_BALANCES_REFRESH = 'eagle:balances:refresh';
export const EVENT_TRANSACTIONS_REFRESH = 'eagle:transactions:refresh';

export default DeviceEventEmitter;
